<div class="mx-auto">
  <div class="opacite mx-auto p-5 mt-4">
    <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="form-inputs">
        <h1><%= f.input :email,
                    required: false,
                    autofocus: true,
                    input_html: { autocomplete: "email" } %></h1>
        <h1><%= f.input :password,
                    required: false,
                    input_html: { autocomplete: "current-password" } %></h1>
        <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
      </div>

      <div class="form-actions">
        <h1><%= f.button :submit, "LOG IN", class: "btn-start" %></h1>
      </div>
    <% end %>
    <%= render "devise/shared/links" %>
  </div>
</div>
